import { describe } from "vitest";
import Image from "./Image";

describe(Image, () => {
  test("Image component is properly rendered", () => {});
  test("Hovered component is rendered", () => {});
  test("Overlay is properly rendered", () => {});
  test("Image is moveable", () => {});
});
