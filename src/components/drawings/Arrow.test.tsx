import { describe } from "vitest";
import Arrow from "./Arrow";

describe(Arrow, () => {
  test("Arrow component is properly rendered", () => {});
  test("Hovered component is rendered", () => {});
  test("Overlay is properly rendered", () => {});
  test("Arrow is moveable", () => {});
});
